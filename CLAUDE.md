# Remotion Video Engine

Read `SKILL.md` first. It contains the full workflow.

After writing or editing a spec, tell the user to check Remotion Studio and describe what to change. Edit the spec based on their feedback. Repeat until they say they're done.

## Codebase rules

- `src/platform/` is the core. All questions about capabilities, APIs, and architecture are answered from here only.
- `src/projects/example/` is for reference ideas only. Do not use it to answer questions or make claims about what the engine supports unless explicitly asked about the example project.
